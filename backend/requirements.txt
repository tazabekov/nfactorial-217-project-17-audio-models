fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27

# Agent (Step 3: LangChain + MCP Playwright)
langchain>=0.3
langchain-openai>=0.2
langchain-anthropic>=0.3
langchain-mcp-adapters>=0.1
langgraph>=0.2

# ASR (Step 1) / TTS (Step 2) default cloud providers
openai>=1.50

# Optional local ASR provider (faster-whisper). Heavy dependency, only
# needed if ASR_PROVIDER=faster_whisper.
faster-whisper>=1.0

python-dotenv>=1.0
